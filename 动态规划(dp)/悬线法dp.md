# 悬线法dp

## 悬线法的概念

- 悬线法用来求**最大全 1 子矩阵**(或者满足某个条件的最大子矩形)
- 把矩阵中的每个点看作一条竖直的"悬线"，维护三个值:
  - `h[i][j]`: 悬线的高度(从当前点向上延伸的长度)
  - `l[i][j]`: 悬线向左最多能扩展到的位置
  - `r[i][j]`: 悬线向右最多能扩展到的位置
- 悬线的宽度就是 `r-l+1`，面积就是 `h*(r-l+1)`
- 时间复杂度 $O(n\times m)$

## 悬线法的实现

- 先从左往右、从右往左各扫一遍当前行，求出本行每个点能向左/右扩展到的最远位置(遇到障碍就重置)
- 再从上往下递推: `h` 直接 +1，`l` 取 max(本行,上一行)，`r` 取 min(本行,上一行)，同时更新答案

```c++
int maxRectangle() {
    int ans = 0;
    for (int i = 1; i <= n; i++) {
        // 初始化 l: 从当前行向左找最近障碍
        for (int j = 1, last = 1; j <= m; j++) {
            if (a[i][j] == 0) { l[i][j] = 1; last = j + 1; }
            else l[i][j] = last;
        }
        // 初始化 r: 从当前行向右找最近障碍
        for (int j = m, last = m; j >= 1; j--) {
            if (a[i][j] == 0) { r[i][j] = m; last = j - 1; }
            else r[i][j] = last;
        }
        // 递推 h, l, r, 并更新答案
        for (int j = 1; j <= m; j++) {
            if (a[i][j] == 0) {
                h[i][j] = 0;
                l[i][j] = 1;
                r[i][j] = m;
            } else {
                h[i][j] = h[i-1][j] + 1;
                l[i][j] = max(l[i][j], l[i-1][j]); // 取更紧约束
                r[i][j] = min(r[i][j], r[i-1][j]);
                ans = max(ans, h[i][j] * (r[i][j] - l[i][j] + 1));
            }
        }
    }
    return ans;
}
```

## 例题

1. [P4147 玉蟾宫](/homework/P4147.cpp)
    > 悬线法求最大全 1 子矩阵的经典题

2. [P1169 棋盘制作](/homework/P1169.cpp)
    > 同时求最大正方形和最大长方形，正方形边长取 `min(h,len)`，长方形就是上面的悬线面积
